---
phase: quick
plan: 260402-v7j
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/scrapers/yad2.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Playwright fallback scrolls the Yad2 page multiple times to trigger lazy-loaded listings"
    - "More listings are extracted than the previous ~3 baseline"
    - "Existing captcha detection and __NEXT_DATA__ parsing still work unchanged"
  artifacts:
    - path: "backend/app/scrapers/yad2.py"
      provides: "fetch_yad2_browser with scroll-to-load logic"
      contains: "scroll"
  key_links:
    - from: "fetch_yad2_browser scroll loop"
      to: "_parse_html_listings"
      via: "final page.content() after scrolling"
      pattern: "page\\.content\\(\\)"
---

<objective>
Increase listing yield from the Yad2 Playwright fallback scraper by adding incremental scroll-down logic to trigger lazy-loaded / infinite-scroll content.

Purpose: Yad2 is a Next.js SPA that lazy-loads listings on scroll. The current implementation grabs HTML after a single 5-second wait with no scrolling, yielding only ~3 listings. Scrolling down in steps will trigger additional content loads and dramatically increase the number of captured listings.

Output: Modified `fetch_yad2_browser` function that scrolls the page repeatedly before extracting HTML.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/app/scrapers/yad2.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scroll-to-load loop to fetch_yad2_browser</name>
  <files>backend/app/scrapers/yad2.py</files>
  <action>
Modify the `fetch_yad2_browser` function (lines 209-299) to add a scroll loop between the initial page load and the `page.content()` call.

**Replace the current post-load logic** (lines 239-241):
```python
await page.goto(url, wait_until="load", timeout=60000)
await page.wait_for_timeout(5000)
content = await page.content()
```

**With this scroll-to-load sequence:**

1. Keep the existing `page.goto(url, wait_until="load", timeout=60000)`.
2. After initial load, wait 3 seconds for initial JS hydration.
3. Run a scroll loop (max 10 iterations) that:
   a. Evaluates `document.body.scrollHeight` to get current page height.
   b. Scrolls to the bottom via `window.scrollTo(0, document.body.scrollHeight)`.
   c. Waits 2 seconds for new content to render.
   d. Checks new `document.body.scrollHeight` — if unchanged from previous iteration, break early (no more content loading).
   e. Logs progress: `[yad2] Scroll {i}: page height {prev_height} -> {new_height}`.
4. After the loop ends, scroll back to top (`window.scrollTo(0, 0)`) and wait 1 second.
5. Then call `content = await page.content()`.

**Implementation detail — use `page.evaluate()` for scroll operations:**
```python
prev_height = await page.evaluate("document.body.scrollHeight")
await page.evaluate("window.scrollTo(0, document.body.scrollHeight)")
await page.wait_for_timeout(2000)
new_height = await page.evaluate("document.body.scrollHeight")
```

**Constants** (define at the top of the function body):
- `MAX_SCROLLS = 10`
- `SCROLL_PAUSE_MS = 2000`
- `INITIAL_WAIT_MS = 3000`

Do NOT touch any other part of the file — the captcha detection block (lines 255-298), `_parse_html_listings`, `_flatten_nextdata_item`, or any other function. Only the section between `page.goto` and the first `content = await page.content()` call changes.
  </action>
  <verify>
    <automated>cd /Users/shahafwieder/HouseFinder && python -c "import ast; ast.parse(open('backend/app/scrapers/yad2.py').read()); print('Syntax OK')" && python -c "from backend.app.scrapers.yad2 import fetch_yad2_browser; print('Import OK')" 2>/dev/null || python -c "import ast; ast.parse(open('backend/app/scrapers/yad2.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>
    - fetch_yad2_browser contains a scroll loop (max 10 iterations) with early exit on unchanged scrollHeight
    - Logs scroll progress at each iteration
    - Initial wait reduced from 5000ms to 3000ms (scrolls handle the rest)
    - All existing captcha detection and HTML parsing logic untouched
    - File parses without syntax errors
  </done>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('backend/app/scrapers/yad2.py').read())"` — no syntax errors
2. Grep for `scrollTo` and `scrollHeight` in the modified function — confirms scroll logic present
3. Grep for `MAX_SCROLLS` — confirms loop bounded
4. The captcha block and `_parse_html_listings` function are unchanged (diff shows no modifications outside `fetch_yad2_browser` lines 238-241 region)
</verification>

<success_criteria>
- fetch_yad2_browser scrolls the page up to 10 times with 2s pauses
- Early exit when page height stops growing
- Scroll progress logged for debugging
- No other functions in yad2.py modified
</success_criteria>

<output>
After completion, create `.planning/quick/260402-v7j-increase-yad2-playwright-scraper-listing/260402-v7j-SUMMARY.md`
</output>
