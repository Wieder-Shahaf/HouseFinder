---
phase: 04-map-web-ui
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - frontend/src/pages/MapView.jsx
  - frontend/src/App.jsx
autonomous: false
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - MAP-05
  - MAP-06
  - MAP-07
  - MAP-08

must_haves:
  truths:
    - "Full app renders on mobile: map with pins, bottom nav, tap-to-detail, filters, favorites"
    - "All Hebrew text flows right-to-left"
    - "Filter changes update pins immediately"
    - "Seen/Favorite mutations persist via API"
    - "Build completes without errors"
  artifacts:
    - path: "frontend/src/App.jsx"
      provides: "Complete app shell with routing, nav, query provider"
    - path: "frontend/src/pages/MapView.jsx"
      provides: "Map with sheets, filters, pin interactions"
  key_links:
    - from: "frontend/src/pages/MapView.jsx"
      to: "/api/listings"
      via: "useListings with filter params"
      pattern: "useListings"
    - from: "frontend/src/components/BottomNav.jsx"
      to: "frontend/src/App.jsx"
      via: "React Router navigation"
      pattern: "navigate"
---

<objective>
Final integration pass and visual verification. Ensure all components work together end-to-end, build passes, and the app is ready for human testing on a mobile device.

Purpose: Catch integration issues between Plans 02 and 03, verify the complete user flow, and get human sign-off on visual/interaction quality.
Output: A fully integrated, build-passing frontend ready for deployment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-map-web-ui/04-CONTEXT.md
@.planning/phases/04-map-web-ui/04-UI-SPEC.md
@.planning/phases/04-map-web-ui/04-02-SUMMARY.md
@.planning/phases/04-map-web-ui/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration verification + build check</name>
  <files>
    frontend/src/pages/MapView.jsx,
    frontend/src/App.jsx
  </files>
  <read_first>
    frontend/src/App.jsx,
    frontend/src/pages/MapView.jsx,
    frontend/src/pages/FavoritesView.jsx,
    frontend/src/components/BottomNav.jsx,
    frontend/src/components/ListingSheet.jsx,
    frontend/src/components/FilterSheet.jsx,
    frontend/src/components/ListingPin.jsx,
    frontend/src/hooks/useListings.js,
    frontend/src/hooks/useListingMutations.js,
    frontend/src/main.jsx,
    frontend/index.html
  </read_first>
  <action>
    1. Read ALL component files to verify integration is correct:
       - App.jsx imports MapView, FavoritesView, BottomNav
       - MapView imports ListingSheet, FilterSheet, ListingPin, useListings, useListingMutations
       - FavoritesView imports FavoriteCard, useListings
       - main.jsx has QueryClientProvider wrapping App
       - index.html has `dir="rtl"` and `lang="he"`

    2. Fix any import path issues, missing props, or wiring problems found during the read-through.

    3. Ensure MapView passes `pb-14` or equivalent bottom padding so content does not hide behind the BottomNav. The map container height should already be `calc(100dvh - 56px)` from Plan 01. Verify this is correct.

    4. Run the full test suite: `cd frontend && npx vitest run`.

    5. Run production build: `cd frontend && npx vite build`. Fix any build errors (unused imports, missing modules, type issues).

    6. Verify the build output exists at `frontend/dist/index.html`.
  </action>
  <verify>
    <automated>cd /Users/shahafwieder/ApartmentFinder/frontend && npx vitest run && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run` exits 0 (all tests pass)
    - `cd frontend && npx vite build` exits 0 (production build succeeds)
    - frontend/dist/index.html exists after build
    - No console errors about missing imports in build output
    - frontend/src/App.jsx imports BottomNav, MapView, FavoritesView
    - frontend/src/main.jsx contains QueryClientProvider
    - frontend/index.html contains 'dir="rtl"' and 'lang="he"'
  </acceptance_criteria>
  <done>All tests pass, production build succeeds, all components wired correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and interaction verification on mobile</name>
  <files>frontend/src/App.jsx</files>
  <action>
    Present the app to the user for visual and interaction testing on a mobile device or Chrome DevTools mobile emulator. The user will verify all MAP-01 through MAP-08 requirements by following the verification steps below. No code changes expected -- this is a human sign-off checkpoint.
  </action>
  <verify>Human confirms all 13 verification steps pass or reports issues to fix.</verify>
  <done>User types "approved" confirming the complete Map Web UI meets all requirements.</done>
  <what-built>
    Complete Map Web UI: interactive Leaflet map centered on Haifa with listing pins, bottom sheet listing detail with seen/favorite actions, filter panel (price/rooms/neighborhood/new-only), favorites page with card list, bottom navigation bar, full Hebrew RTL layout.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd frontend && npm run dev` (ensure backend is running at localhost:8000)
    2. Open http://localhost:5173 on your phone (or phone emulator in Chrome DevTools)
    3. Verify MAP-01: Map loads with OpenStreetMap tiles and listing pins visible
    4. Verify MAP-03: Map is centered on Haifa (Carmel, Downtown, Neve Shanan visible)
    5. Verify MAP-02: Tap a pin -- bottom sheet slides up showing price, rooms, size, address, contact, date, source badge, link
    6. Verify D-03: Tap the map background -- sheet dismisses
    7. Verify MAP-05: Tap "ראיתי" on a listing -- pin changes to grey/faded
    8. Verify MAP-06: Tap "שמור" on a listing -- pin changes to red heart
    9. Verify MAP-04: Tap filter icon (top-right) -- filter panel opens. Adjust price slider, toggle rooms, toggle neighborhoods, toggle "חדשות בלבד" -- pins update live
    10. Verify D-09: Tap "שמורים" in bottom nav -- favorites page loads. Tap "מפה" -- back to map
    11. Verify MAP-06: Favorites page shows saved listings as cards with "הדירות השמורות שלי" header
    12. Verify MAP-07: All text flows right-to-left, buttons align correctly for RTL
    13. Verify MAP-08: All buttons/tabs are easy to tap (44px+ targets), map fills screen properly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All Vitest tests pass
- Vite production build succeeds
- Human verified on mobile device
</verification>

<success_criteria>
- Complete end-to-end user flow works: map loads, pin tap opens detail, seen/favorite work, filters update pins, favorites page accessible
- Hebrew RTL layout correct throughout
- Mobile-responsive and usable on smartphone
- Human approval received
</success_criteria>

<output>
After completion, create `.planning/phases/04-map-web-ui/04-04-SUMMARY.md`
</output>
