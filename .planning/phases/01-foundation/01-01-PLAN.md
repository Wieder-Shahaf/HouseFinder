---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/database.py
  - backend/app/config.py
  - backend/app/models/__init__.py
  - backend/app/models/listing.py
  - backend/app/schemas/__init__.py
  - backend/app/schemas/listing.py
  - backend/app/routers/__init__.py
  - backend/app/routers/listings.py
  - backend/alembic/env.py
  - backend/alembic/versions/0001_initial_schema.py
  - backend/alembic.ini
  - backend/requirements.txt
  - backend/Dockerfile
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_database.py
  - backend/tests/test_api.py
  - backend/pytest.ini
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - INFRA-04

must_haves:
  truths:
    - "SQLite database has a listings table with all 19+ columns defined in D-01 through D-04"
    - "Inserting a duplicate (source, source_id) pair raises IntegrityError (D-05)"
    - "FastAPI /api/health endpoint returns HTTP 200 with JSON body"
    - "pytest suite passes with all database and API smoke tests green"
  artifacts:
    - path: "backend/app/models/listing.py"
      provides: "Listing SQLAlchemy model with all columns per D-02, D-03, D-04"
      contains: "UniqueConstraint"
    - path: "backend/app/database.py"
      provides: "Async engine, session factory, Base declarative"
      exports: ["engine", "async_session_factory", "Base", "get_db"]
    - path: "backend/app/main.py"
      provides: "FastAPI app with /api/health endpoint"
      contains: "FastAPI"
    - path: "backend/app/config.py"
      provides: "pydantic-settings BaseSettings for .env loading"
      contains: "BaseSettings"
    - path: "backend/alembic/env.py"
      provides: "Async-aware Alembic migration environment with render_as_batch=True"
      contains: "render_as_batch"
    - path: "backend/tests/test_database.py"
      provides: "Unit tests for schema columns and dedup constraint"
      contains: "test_dedup_constraint"
  key_links:
    - from: "backend/app/models/listing.py"
      to: "backend/app/database.py"
      via: "imports Base from database"
      pattern: "from.*database import Base"
    - from: "backend/alembic/env.py"
      to: "backend/app/models/listing.py"
      via: "imports Listing model for autogenerate"
      pattern: "from app.models"
    - from: "backend/app/main.py"
      to: "backend/app/config.py"
      via: "loads settings for DATABASE_URL"
      pattern: "from.*config import"
---

<objective>
Create the complete Python backend skeleton: FastAPI app, SQLAlchemy async models with the full listings schema (all columns per D-01 through D-05), Alembic async migrations, pydantic-settings config, test scaffold (Wave 0), and backend Dockerfile. This plan produces the data layer that every subsequent phase depends on.

Purpose: Establish the database schema, migration pipeline, and test infrastructure before any scraper or API work begins.
Output: Working backend that passes pytest (schema validation + dedup constraint + health endpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend project structure with FastAPI, SQLAlchemy, config, and test scaffold (Wave 0)</name>
  <files>
    backend/app/__init__.py,
    backend/app/main.py,
    backend/app/database.py,
    backend/app/config.py,
    backend/app/models/__init__.py,
    backend/app/models/listing.py,
    backend/app/schemas/__init__.py,
    backend/app/schemas/listing.py,
    backend/app/routers/__init__.py,
    backend/app/routers/listings.py,
    backend/requirements.txt,
    backend/Dockerfile,
    backend/pytest.ini,
    backend/tests/__init__.py,
    backend/tests/conftest.py,
    backend/tests/test_database.py,
    backend/tests/test_api.py,
    .env.example,
    .gitignore
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/phases/01-foundation/01-CONTEXT.md
    CLAUDE.md
  </read_first>
  <action>
Create the full backend directory tree. All files listed below with their exact content specifications:

**backend/requirements.txt** — exact packages:
```
fastapi==0.135.2
uvicorn[standard]==0.30.6
sqlalchemy==2.0.48
aiosqlite==0.22.1
alembic==1.18.4
pydantic==2.11.5
pydantic-settings>=2.0
apscheduler==3.11.2
python-dotenv>=1.0
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
```

**backend/Dockerfile:**
```dockerfile
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

**backend/app/__init__.py** — empty file.

**backend/app/config.py** — pydantic-settings BaseSettings (per RESEARCH Pattern):
- `database_url: str = "sqlite+aiosqlite:////data/listings.db"`
- `twilio_account_sid: str = ""`
- `twilio_auth_token: str = ""`
- `twilio_whatsapp_from: str = ""`
- `llm_api_key: str = ""`
- `app_url: str = "http://localhost:8000"`
- Inner `class Config:` with `env_file = ".env"` and `env_file_encoding = "utf-8"`
- Module-level `settings = Settings()` singleton

**backend/app/database.py** — async engine + session factory (per RESEARCH Pattern 1):
- Import `create_async_engine`, `AsyncSession`, `async_sessionmaker` from sqlalchemy.ext.asyncio
- Import `DeclarativeBase` from sqlalchemy.orm
- Import `settings` from `app.config`
- `engine = create_async_engine(settings.database_url, echo=False)`
- `async_session_factory = async_sessionmaker(engine, expire_on_commit=False)`
- `class Base(DeclarativeBase): pass`
- `async def get_db() -> AsyncSession:` — async generator yielding session from factory

**backend/app/models/__init__.py** — `from .listing import Listing` (re-export for Alembic autogenerate)

**backend/app/models/listing.py** — Full Listing model per D-01 through D-05 (copy from RESEARCH code example verbatim):
- `id: Mapped[int]` — Integer, primary_key, autoincrement
- Infrastructure columns (D-02): `source` (String(50), NOT NULL), `source_id` (String(255), NOT NULL), `lat` (Float, nullable), `lng` (Float, nullable), `is_seen` (Boolean, default=False, NOT NULL), `is_favorited` (Boolean, default=False, NOT NULL), `raw_data` (Text, nullable), `llm_confidence` (Float, nullable), `dedup_fingerprint` (String(64), nullable)
- Listing data columns (D-03): `title` (String(500), nullable), `price` (Integer, nullable), `rooms` (Float, nullable), `size_sqm` (Integer, nullable), `address` (String(500), nullable), `contact_info` (String(500), nullable), `post_date` (DateTime, nullable), `url` (String(1000), nullable), `source_badge` (String(50), nullable)
- Metadata columns (D-04): `created_at` (DateTime, default=datetime.utcnow, NOT NULL), `updated_at` (DateTime, default=datetime.utcnow, onupdate=datetime.utcnow, NOT NULL), `is_active` (Boolean, default=True, NOT NULL)
- `__table_args__` with `UniqueConstraint("source", "source_id", name="uq_listing_source_source_id")` (D-05)

**backend/app/schemas/__init__.py** — empty.

**backend/app/schemas/listing.py** — Pydantic response schema:
- `class ListingResponse(BaseModel):` with all fields from the model, `model_config = ConfigDict(from_attributes=True)`

**backend/app/routers/__init__.py** — empty.

**backend/app/routers/listings.py** — Stub router:
- `router = APIRouter(prefix="/api/listings", tags=["listings"])`
- One stub `GET /` endpoint returning empty list (placeholder for Phase 3)

**backend/app/main.py** — FastAPI app factory (per RESEARCH Pattern):
- Import FastAPI, lifespan context manager
- `@asynccontextmanager async def lifespan(app):` — yield (no table creation; Alembic handles schema)
- `app = FastAPI(title="ApartmentFinder API", lifespan=lifespan)`
- `@app.get("/api/health")` returning `{"status": "ok"}`
- Include listings router via `app.include_router(router)`

**backend/pytest.ini:**
```ini
[pytest]
asyncio_mode = auto
```

**backend/tests/__init__.py** — empty.

**backend/tests/conftest.py** — shared async test fixtures:
- Create an in-memory SQLite async engine: `create_async_engine("sqlite+aiosqlite://", echo=True)`
- Fixture `db_engine` that creates all tables from `Base.metadata` using `conn.run_sync(Base.metadata.create_all)`, yields engine, then drops all
- Fixture `db_session` that creates an `async_sessionmaker` from the engine, yields a session, then closes
- Fixture `client` using `httpx.AsyncClient` with `ASGITransport(app=app)` for testing FastAPI endpoints

**backend/tests/test_database.py** — Tests for DATA-01 and DATA-02:
- `test_listings_table_columns`: Use `inspect(conn).get_columns("listings")` via `run_sync` to verify all 19+ column names exist: `id`, `source`, `source_id`, `lat`, `lng`, `is_seen`, `is_favorited`, `raw_data`, `llm_confidence`, `dedup_fingerprint`, `title`, `price`, `rooms`, `size_sqm`, `address`, `contact_info`, `post_date`, `url`, `source_badge`, `created_at`, `updated_at`, `is_active`
- `test_dedup_constraint`: Insert two Listing rows with identical `source="yad2"` and `source_id="123"`, assert second insert raises `IntegrityError`
- `test_nullable_fields`: Insert a Listing with only `source` and `source_id` filled, verify it succeeds (all other fields nullable)

**backend/tests/test_api.py** — Smoke test for INFRA-01:
- `test_health_endpoint`: `GET /api/health` returns status 200, body contains `{"status": "ok"}`

**.env.example:**
```
DATABASE_URL=sqlite+aiosqlite:////data/listings.db
TWILIO_ACCOUNT_SID=your_twilio_sid_here
TWILIO_AUTH_TOKEN=your_twilio_token_here
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
LLM_API_KEY=your_llm_api_key_here
APP_URL=https://yourdomain.me
```

**.gitignore** — append (do not overwrite if exists) these entries:
```
.env
__pycache__/
*.pyc
*.pyo
backend/data/
node_modules/
frontend/dist/
.vite/
```
  </action>
  <verify>
    <automated>cd /Users/shahafwieder/ApartmentFinder && docker compose -f docker-compose.yml -f docker-compose.dev.yml run --rm backend pytest tests/ -x -q 2>/dev/null || (cd backend && pip install -r requirements.txt -q && pytest tests/ -x -q)</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/models/listing.py contains `class Listing(Base):`
    - backend/app/models/listing.py contains `UniqueConstraint("source", "source_id", name="uq_listing_source_source_id")`
    - backend/app/models/listing.py contains all 22 column definitions: id, source, source_id, lat, lng, is_seen, is_favorited, raw_data, llm_confidence, dedup_fingerprint, title, price, rooms, size_sqm, address, contact_info, post_date, url, source_badge, created_at, updated_at, is_active
    - backend/app/database.py contains `create_async_engine` and `async_session_factory`
    - backend/app/database.py contains `class Base(DeclarativeBase)`
    - backend/app/config.py contains `class Settings(BaseSettings):`
    - backend/app/config.py contains `database_url` and `twilio_account_sid` and `llm_api_key`
    - backend/app/main.py contains `@app.get("/api/health")`
    - backend/app/main.py contains `app.include_router`
    - backend/requirements.txt contains `fastapi==0.135.2` and `sqlalchemy==2.0.48` and `aiosqlite==0.22.1` and `alembic==1.18.4` and `pytest`
    - backend/Dockerfile contains `FROM python:3.12-slim`
    - backend/pytest.ini contains `asyncio_mode = auto`
    - backend/tests/conftest.py contains `sqlite+aiosqlite://`
    - backend/tests/test_database.py contains `test_listings_table_columns` and `test_dedup_constraint`
    - backend/tests/test_api.py contains `test_health_endpoint`
    - .env.example contains `DATABASE_URL=` and `TWILIO_ACCOUNT_SID=` and `LLM_API_KEY=`
    - .gitignore contains `.env` and `__pycache__`
  </acceptance_criteria>
  <done>Backend project structure exists with all files. Listing model has all 22 columns per D-01/D-02/D-03/D-04 and UniqueConstraint per D-05. Config loads from .env. Health endpoint defined. Test scaffold ready for pytest execution.</done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic async migration and verify full schema + dedup constraint</name>
  <files>
    backend/alembic.ini,
    backend/alembic/env.py,
    backend/alembic/script.py.mako,
    backend/alembic/versions/0001_initial_schema.py
  </files>
  <read_first>
    backend/app/models/listing.py
    backend/app/database.py
    backend/app/config.py
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
Initialize Alembic with async template and create the initial migration.

**backend/alembic.ini:**
```ini
[alembic]
script_location = alembic
sqlalchemy.url = sqlite+aiosqlite:////data/listings.db

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
```

**backend/alembic/env.py** — async-aware migration environment (per RESEARCH Pattern 2):
- Import `asyncio`, `fileConfig` from logging.config, `create_async_engine` from sqlalchemy.ext.asyncio, `context` from alembic
- `config = context.config`
- `fileConfig(config.config_file_name)`
- Import `from app.models.listing import Listing  # noqa` and `from app.database import Base`
- `target_metadata = Base.metadata`
- `def do_run_migrations(connection):` — call `context.configure(connection=connection, target_metadata=target_metadata, render_as_batch=True)` then `context.begin_transaction()` then `context.run_migrations()`
- `def run_migrations_online():` — create engine from `config.get_main_option("sqlalchemy.url")`, run async migrations with `connection.run_sync(do_run_migrations)`, dispose engine
- Call `run_migrations_online()` at module level

**backend/alembic/script.py.mako** — standard Alembic template (use default from `alembic init`).

**backend/alembic/versions/0001_initial_schema.py** — hand-written initial migration:
- `revision = "0001"`
- `down_revision = None`
- `depends_on = None`
- `def upgrade():` — use `op.create_table("listings", ...)` with ALL 22 columns matching the Listing model exactly:
  - `sa.Column("id", sa.Integer(), primary_key=True, autoincrement=True)`
  - `sa.Column("source", sa.String(50), nullable=False)`
  - `sa.Column("source_id", sa.String(255), nullable=False)`
  - `sa.Column("lat", sa.Float(), nullable=True)`
  - `sa.Column("lng", sa.Float(), nullable=True)`
  - `sa.Column("is_seen", sa.Boolean(), nullable=False, server_default="0")`
  - `sa.Column("is_favorited", sa.Boolean(), nullable=False, server_default="0")`
  - `sa.Column("raw_data", sa.Text(), nullable=True)`
  - `sa.Column("llm_confidence", sa.Float(), nullable=True)`
  - `sa.Column("dedup_fingerprint", sa.String(64), nullable=True)`
  - `sa.Column("title", sa.String(500), nullable=True)`
  - `sa.Column("price", sa.Integer(), nullable=True)`
  - `sa.Column("rooms", sa.Float(), nullable=True)`
  - `sa.Column("size_sqm", sa.Integer(), nullable=True)`
  - `sa.Column("address", sa.String(500), nullable=True)`
  - `sa.Column("contact_info", sa.String(500), nullable=True)`
  - `sa.Column("post_date", sa.DateTime(), nullable=True)`
  - `sa.Column("url", sa.String(1000), nullable=True)`
  - `sa.Column("source_badge", sa.String(50), nullable=True)`
  - `sa.Column("created_at", sa.DateTime(), nullable=False, server_default=sa.func.now())`
  - `sa.Column("updated_at", sa.DateTime(), nullable=False, server_default=sa.func.now())`
  - `sa.Column("is_active", sa.Boolean(), nullable=False, server_default="1")`
  - `sa.UniqueConstraint("source", "source_id", name="uq_listing_source_source_id")`
- `def downgrade():` — `op.drop_table("listings")`

After creating the migration, run `alembic upgrade head` against a test database to verify the migration applies cleanly, then run the full pytest suite to confirm schema tests pass.
  </action>
  <verify>
    <automated>cd /Users/shahafwieder/ApartmentFinder/backend && DATABASE_URL="sqlite+aiosqlite:///./test_migration.db" python -c "import asyncio; from alembic.config import Config; from alembic import command; c = Config('alembic.ini'); c.set_main_option('sqlalchemy.url', 'sqlite+aiosqlite:///./test_migration.db'); command.upgrade(c, 'head'); print('Migration OK')" && rm -f test_migration.db && pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/alembic.ini contains `script_location = alembic`
    - backend/alembic.ini contains `sqlalchemy.url = sqlite+aiosqlite`
    - backend/alembic/env.py contains `render_as_batch=True`
    - backend/alembic/env.py contains `from app.models.listing import Listing`
    - backend/alembic/env.py contains `target_metadata = Base.metadata`
    - backend/alembic/env.py contains `run_sync(do_run_migrations)`
    - backend/alembic/versions/0001_initial_schema.py contains `op.create_table("listings"`
    - backend/alembic/versions/0001_initial_schema.py contains `UniqueConstraint("source", "source_id"`
    - backend/alembic/versions/0001_initial_schema.py contains all 22 column names
    - backend/alembic/versions/0001_initial_schema.py contains `def downgrade` with `op.drop_table("listings")`
    - Alembic `upgrade head` completes without error
    - All pytest tests pass (test_listings_table_columns, test_dedup_constraint, test_health_endpoint)
  </acceptance_criteria>
  <done>Alembic migration creates the listings table with all 22 columns and the dedup UniqueConstraint. Migration runs cleanly. All pytest tests pass confirming schema correctness and constraint enforcement.</done>
</task>

</tasks>

<verification>
- `pytest tests/ -x -q` exits 0 with all tests passing
- `test_listings_table_columns` confirms all 22 column names present in the listings table
- `test_dedup_constraint` confirms IntegrityError on duplicate (source, source_id)
- `test_health_endpoint` confirms GET /api/health returns 200
- Alembic migration applies and rolls back cleanly
</verification>

<success_criteria>
- Backend project structure exists with all Python files
- Listing model has 22 columns matching D-01 through D-04 exactly
- UniqueConstraint on (source, source_id) per D-05
- Alembic async migration creates schema correctly
- pytest suite passes all tests (DATA-01, DATA-02, INFRA-04 smoke)
- .env.example documents all required environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
