---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/src/main.jsx
  - frontend/src/App.jsx
  - frontend/src/index.css
  - frontend/vite.config.js
  - frontend/package.json
  - frontend/Dockerfile
  - frontend/nginx.conf
  - nginx/nginx.dev.conf
  - nginx/nginx.prod.conf
  - docker-compose.yml
  - docker-compose.dev.yml
  - docker-compose.prod.yml
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "docker compose -f docker-compose.yml -f docker-compose.dev.yml config validates without errors"
    - "Frontend renders a Hebrew RTL page with Noto Sans Hebrew font"
    - "Nginx prod config proxies /api/ to backend and serves SPA fallback"
    - "docker compose -f docker-compose.yml -f docker-compose.dev.yml up starts both backend and frontend containers"
  artifacts:
    - path: "frontend/index.html"
      provides: "HTML entry with lang=he dir=rtl and Noto Sans Hebrew"
      contains: "dir=\"rtl\""
    - path: "frontend/vite.config.js"
      provides: "Vite 8 config with React and Tailwind v4 plugins"
      contains: "@tailwindcss/vite"
    - path: "frontend/src/index.css"
      provides: "Tailwind v4 CSS entry point"
      contains: "@import \"tailwindcss\""
    - path: "docker-compose.yml"
      provides: "Base Compose with backend and frontend services"
      contains: "services:"
    - path: "docker-compose.dev.yml"
      provides: "Dev overrides with hot-reload volumes and exposed ports"
      contains: "--reload"
    - path: "docker-compose.prod.yml"
      provides: "Prod overrides with Nginx and Certbot"
      contains: "certbot"
    - path: "nginx/nginx.prod.conf"
      provides: "Nginx reverse proxy config for production"
      contains: "proxy_pass http://backend:8000"
  key_links:
    - from: "docker-compose.yml"
      to: "backend/Dockerfile"
      via: "build context ./backend"
      pattern: "context:.*backend"
    - from: "docker-compose.yml"
      to: "frontend/Dockerfile"
      via: "build context ./frontend"
      pattern: "context:.*frontend"
    - from: "nginx/nginx.prod.conf"
      to: "backend service"
      via: "proxy_pass to backend:8000"
      pattern: "proxy_pass.*backend:8000"
---

<objective>
Create the React 19 + Vite 8 + Tailwind v4 frontend scaffold with Hebrew RTL from day one, plus all Docker Compose files (base, dev, prod) and Nginx configs. After this plan, `docker compose config` validates and the full stack can be started.

Purpose: Establish the containerized dev/prod environment (per D-06 through D-08) and the frontend skeleton that Phase 4 builds the map UI upon.
Output: Working Docker Compose multi-file setup and a minimal Hebrew RTL React app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create React 19 + Vite 8 + Tailwind v4 frontend scaffold with Hebrew RTL</name>
  <files>
    frontend/index.html,
    frontend/src/main.jsx,
    frontend/src/App.jsx,
    frontend/src/index.css,
    frontend/vite.config.js,
    frontend/package.json,
    frontend/Dockerfile,
    frontend/nginx.conf,
    frontend/.gitignore
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/phases/01-foundation/01-CONTEXT.md
    CLAUDE.md
  </read_first>
  <action>
Create the frontend directory with all files. Use `npm create vite@latest frontend -- --template react` as the starting point, then modify files to match these exact specifications.

**frontend/package.json** — after scaffolding, ensure these dependencies:
- `react`: `^19.0.0`
- `react-dom`: `^19.0.0`
- `react-leaflet`: `^5.0.0`
- `leaflet`: `^1.9.0`
- `@tanstack/react-query`: `^5.0.0`
- `react-router-dom`: `^7.0.0`
- devDependencies: `@vitejs/plugin-react`: `^6.0.0`, `tailwindcss`: `^4.0.0`, `@tailwindcss/vite`: `^4.0.0`, `vite`: `^8.0.0`

Run `npm install` inside frontend/ to generate lock file.

**frontend/vite.config.js** — per RESEARCH Pattern 3:
```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: {
    host: true,
    port: 5173,
  },
})
```

**frontend/src/index.css** — Tailwind v4 entry (NOT v3 directives):
```css
@import "tailwindcss";

@theme {
  --font-family-sans: 'Noto Sans Hebrew', sans-serif;
}
```
Do NOT create a `tailwind.config.js` file. Do NOT create a `postcss.config.js` file. Tailwind v4 uses CSS-based config via `@theme` directives.

**frontend/index.html** — Hebrew RTL from day one per RESEARCH Pattern 3:
```html
<!DOCTYPE html>
<html lang="he" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Hebrew:wght@400;500;700&display=swap" rel="stylesheet" />
    <title>ApartmentFinder - דירות להשכרה בחיפה</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

**frontend/src/main.jsx:**
```jsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import App from './App.jsx'
import './index.css'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

**frontend/src/App.jsx** — minimal Hebrew placeholder:
```jsx
function App() {
  return (
    <div className="min-h-screen bg-gray-50 flex items-center justify-center font-sans">
      <div className="text-center p-8">
        <h1 className="text-3xl font-bold text-gray-900 mb-4">ApartmentFinder</h1>
        <p className="text-lg text-gray-600">דירות להשכרה בחיפה</p>
        <p className="text-sm text-gray-400 mt-2">המערכת בבנייה</p>
      </div>
    </div>
  )
}

export default App
```

**frontend/Dockerfile** — multi-stage build (per RESEARCH code example):
```dockerfile
FROM node:22-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:1.27-alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```

**frontend/nginx.conf** — simple SPA fallback for the prod container:
```nginx
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

**frontend/.gitignore:**
```
node_modules/
dist/
.vite/
```
  </action>
  <verify>
    <automated>cd /Users/shahafwieder/ApartmentFinder/frontend && npm install && npm run build && echo "Frontend build OK"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/index.html contains `lang="he"` and `dir="rtl"`
    - frontend/index.html contains `Noto+Sans+Hebrew`
    - frontend/index.html contains `ApartmentFinder`
    - frontend/vite.config.js contains `@tailwindcss/vite`
    - frontend/vite.config.js contains `react()` and `tailwindcss()`
    - frontend/src/index.css contains `@import "tailwindcss"`
    - frontend/src/index.css does NOT contain `@tailwind base` (that is v3 syntax)
    - frontend/src/App.jsx contains Hebrew text `דירות להשכרה בחיפה`
    - frontend/package.json contains `react` version starting with `^19`
    - frontend/package.json contains `tailwindcss` in devDependencies
    - frontend/package.json contains `@tailwindcss/vite` in devDependencies
    - frontend/Dockerfile contains `FROM node:22-alpine`
    - frontend/Dockerfile contains `FROM nginx:1.27-alpine`
    - No `tailwind.config.js` file exists in frontend/
    - No `postcss.config.js` file exists in frontend/
    - `npm run build` exits 0 and produces frontend/dist/index.html
  </acceptance_criteria>
  <done>Frontend scaffold exists with React 19, Vite 8, Tailwind v4 (CSS-based config, no JS config), Hebrew RTL, Noto Sans Hebrew font, and a multi-stage Dockerfile. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create Docker Compose files (base, dev, prod) and Nginx configs per D-06/D-07/D-08</name>
  <files>
    docker-compose.yml,
    docker-compose.dev.yml,
    docker-compose.prod.yml,
    nginx/nginx.dev.conf,
    nginx/nginx.prod.conf
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/phases/01-foundation/01-CONTEXT.md
    backend/Dockerfile
    frontend/Dockerfile
    .env.example
  </read_first>
  <action>
Create the Docker Compose multi-file strategy per D-06, D-07, D-08 and RESEARCH Pattern 4/5.

**docker-compose.yml** (base — shared service definitions):
```yaml
services:
  backend:
    build:
      context: ./backend
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////data/listings.db
    env_file:
      - .env
    volumes:
      - sqlite_data:/data
    networks:
      - app_network

  frontend:
    build:
      context: ./frontend
    networks:
      - app_network

volumes:
  sqlite_data:

networks:
  app_network:
```

**docker-compose.dev.yml** (dev overrides per D-07, D-08):
```yaml
services:
  backend:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./backend:/app
      - sqlite_data:/data
    ports:
      - "8000:8000"

  frontend:
    command: npm run dev -- --host
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "5173:5173"
```

**docker-compose.prod.yml** (prod overrides per D-13):
```yaml
services:
  backend:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2
    restart: unless-stopped

  nginx:
    image: nginx:1.27-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.prod.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot_webroot:/var/www/certbot:ro
      - certbot_certs:/etc/letsencrypt:ro
    depends_on:
      - backend
      - frontend
    networks:
      - app_network
    restart: unless-stopped

  certbot:
    image: certbot/certbot
    volumes:
      - certbot_webroot:/var/www/certbot
      - certbot_certs:/etc/letsencrypt
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
    networks:
      - app_network

volumes:
  sqlite_data:
  certbot_webroot:
  certbot_certs:

networks:
  app_network:
```

**nginx/nginx.dev.conf:**
```nginx
server {
    listen 80;

    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location / {
        proxy_pass http://frontend:5173;
        proxy_set_header Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

**nginx/nginx.prod.conf** (per RESEARCH Pattern 5):
```nginx
server {
    listen 80;
    server_name _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name _;

    ssl_certificate /etc/letsencrypt/live/yourdomain.me/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yourdomain.me/privkey.pem;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

After creating all files, validate with: `docker compose -f docker-compose.yml -f docker-compose.dev.yml config --quiet`
  </action>
  <verify>
    <automated>cd /Users/shahafwieder/ApartmentFinder && docker compose -f docker-compose.yml -f docker-compose.dev.yml config --quiet && echo "Compose config valid"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains `services:` with `backend:` and `frontend:` definitions
    - docker-compose.yml contains `sqlite_data:` under volumes
    - docker-compose.yml contains `app_network:` under networks
    - docker-compose.yml contains `env_file:` referencing `.env`
    - docker-compose.dev.yml contains `uvicorn` with `--reload`
    - docker-compose.dev.yml contains `ports:` with `"8000:8000"` and `"5173:5173"`
    - docker-compose.dev.yml contains `./backend:/app` volume mount
    - docker-compose.prod.yml contains `nginx:` service with image `nginx:1.27-alpine`
    - docker-compose.prod.yml contains `certbot:` service
    - docker-compose.prod.yml contains `certbot_certs:` and `certbot_webroot:` volumes
    - nginx/nginx.dev.conf contains `proxy_pass http://backend:8000`
    - nginx/nginx.prod.conf contains `proxy_pass http://backend:8000`
    - nginx/nginx.prod.conf contains `ssl_certificate`
    - nginx/nginx.prod.conf contains `try_files $uri $uri/ /index.html`
    - nginx/nginx.prod.conf contains `/.well-known/acme-challenge/`
    - `docker compose -f docker-compose.yml -f docker-compose.dev.yml config --quiet` exits 0
  </acceptance_criteria>
  <done>Docker Compose multi-file setup validates. Base defines services and shared network. Dev overrides add hot-reload and exposed ports (D-07, D-08). Prod overrides add Nginx reverse proxy with SSL termination and Certbot auto-renewal (D-13). Both Nginx configs proxy /api/ to backend:8000.</done>
</task>

</tasks>

<verification>
- `docker compose -f docker-compose.yml -f docker-compose.dev.yml config --quiet` exits 0
- `docker compose -f docker-compose.yml -f docker-compose.prod.yml config --quiet` exits 0
- `npm run build` in frontend/ exits 0
- frontend/dist/index.html contains `lang="he"` and `dir="rtl"`
- No `tailwind.config.js` or `postcss.config.js` exists
</verification>

<success_criteria>
- Frontend scaffold builds successfully with React 19 + Vite 8 + Tailwind v4
- Hebrew RTL (`lang="he" dir="rtl"`) set on HTML element
- Noto Sans Hebrew font loaded from Google Fonts
- Docker Compose validates for both dev and prod configurations
- Dev mode supports hot-reload with volume mounts (D-08)
- Prod mode includes Nginx + Certbot for SSL (D-13)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
